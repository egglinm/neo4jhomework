Run node index.js from the terminal. 
This will open a window in Apollo Graph QL, which contains two different tabs.
The first tab contains a mutation that allows the user to create new employees by providing a name and id number.
There are several name and ID numbers inputted currently as an example.
In the second tab,  there is a query that returns all employees in the database.